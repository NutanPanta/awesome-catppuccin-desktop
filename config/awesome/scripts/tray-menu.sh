#!/usr/bin/env bash
# Legacy helper — tray menus are handled in Lua (tray-menu.lua).
# snixembed + the hidden systray host in taskbar-pill.lua keep tray apps alive.
echo "Tray menus are handled by Awesome (tray-menu.lua). Reload Awesome and right-click a pill icon."
exit 0
